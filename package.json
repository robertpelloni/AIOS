{
  "name": "super-ai-plugin",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "start": "pnpm --filter @super-ai/core start",
    "dev": "pnpm --filter @super-ai/core dev",
    "build": "pnpm --filter \"./packages/*\" build",
    "test": "echo \"Error: no test specified\" && exit 0",
    "lint": "echo \"Linting...\"",
    "start:all": "pnpm run build && pnpm start"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
